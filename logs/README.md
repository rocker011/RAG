# Logs

This directory stores archived historical logs that should not stay in the repository root or in `evaluation/` root.

## Current usage

- `archive/root/`: historical root-level logs
- `archive/evaluation/`: historical evaluation-level logs

For method- and dataset-specific logs, prefer:

- `evaluation/logs/<dataset>/<method>/`
